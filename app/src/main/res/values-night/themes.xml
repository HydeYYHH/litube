<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode -->
    <style name="Base.Theme.YoutubeLite" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- 1. Main Branding Colors (FORCING GREEN) -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimaryContainer">@color/white</item>

        <!-- 2. Secondary/Accent Colors (Controls boxes, switches, progress bars) -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSecondaryContainer">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondaryContainer">@color/white</item>

        <!-- 3. UI Elements (The "Boxes" and Text Highlights) -->
        <item name="colorOutline">@color/colorSecondary</item>
        <item name="colorControlActivated">@color/colorSecondary</item>
        <item name="colorControlHighlight">@color/colorSecondary</item>

        <!-- 4. System Navigation -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Ensuring the main theme inherits from our Base -->
    <style name="Theme.YoutubeLite" parent="Base.Theme.YoutubeLite" />
</resources>